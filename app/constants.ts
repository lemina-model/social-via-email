/** Global keyword used for app identity, metadata, branding, etc. */
export const APP_KEYWORD = "social-via-email";

export const SESSION_COOKIE_NAME = "sve_session";

/** Set when /loading has finished; nav links stay disabled until then. */
export const LOADING_COMPLETE_COOKIE_NAME = "sve_loading_done";

/** sessionStorage key for passing Gmail token from sign-in to loading page (one-time use). */
export const GMAIL_TOKEN_STORAGE_KEY = "sve_gmail_token";
