/** Global keywords used for app identity, metadata, branding, etc. */
export const APP_NAME = "Social via Email";
export const APP_CODE_NAME = "social-via-email";

/** Subject of the email storing a list of posts */
export const REPO_OF_POSTS = "repository-of-posts";

/** Subject of the email storing a list of following and a list of followers */
export const REPO_OF_PEOPLE = "repository-of-people";

/** Path to the optional seed file for the post repository (fetched at runtime). */
export const TEST_SEED_POSTS_FILE = "/test-seed-posts.json";

/** Path to the optional seed file for the people repository (fetched at runtime). */
export const TEST_SEED_PEOPLE_FILE = "/test-seed-people.json";
