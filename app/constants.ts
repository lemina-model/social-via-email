/** Global keywords used for app identity, metadata, branding, etc. */
export const APP_NAME = "Social via Email";
export const APP_CODE_NAME = "social-via-email";

/** App emails should be moved to this folder so they won't mess up the user's inbox. */
export const EMAIL_FOLDER_FOR_INBOX = "social-via-email-inbox";
/** Emails containing data for the app should stay in this folder. */
export const EMAIL_FOLDER_FOR_DATA = "social-via-email-data";

/** Subject of the email storing a memory dump */
export const EMAIL_SUBJECT_FOR_REPO = "repository-of-data";

/** Path to the optional seed data file. */
export const FILE_OF_SEED_DATA = "/test-seed-data.json";
